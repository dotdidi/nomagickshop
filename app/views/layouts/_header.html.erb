<header class="navbar navbar-fixed-top">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_url %></li>
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle= "dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_url(current_user)  %></li>
              <li class="divider"></li>
              <li><%= link_to "Log out", bye_url, method: "delete"  %></li>
            </ul>
          <% else %>
            <li><%= link_to "Log in", welcome_url  %>
            <li><%= link_to "Newcommer", new_user_url %></li>
          </li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle= "dropdown">
            Your Cart <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li class="divider"></li>
            <li><%= link_to "Check Out", '#'  %></li>
          </ul>
        </li>
      </ul>
    </nav>
  </div>
</header>
